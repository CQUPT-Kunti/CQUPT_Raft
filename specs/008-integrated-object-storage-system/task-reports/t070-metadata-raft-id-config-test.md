# T070 MetadataNode raft_id generated by config 测试报告

## 1. 修改了哪些文件

- `tests/cluster_config_test.cpp`
- `specs/008-integrated-object-storage-system/tasks.md`
- `specs/008-integrated-object-storage-system/task-reports/t070-metadata-raft-id-config-test.md`

未修改：

- `tests/CMakeLists.txt`
- 生产代码
- `proto/`
- app 入口
- `common-risk-notes.md`
- `risk-register.md`

## 2. T070 的 MetadataNode raft_id generated by config 测试做了什么

本次只新增 cluster config 测试，没有实现或修改任何生产逻辑。

新增覆盖包括：

- `cluster_config_generation_test.T070MetadataRaftIdsStayStableDistinctAndMatchInitialMembership`
  - 覆盖 1/3/5/7 voter 场景。
  - 为 MetadataNode 指定一组固定但非数字语义的 `node_id`，验证 `raft_id` 仍按配置生成、与 `node_id` 语义分离。
  - 验证同一 generation request 重复生成时，`node_id -> raft_id` 映射稳定。
  - 验证 `initial_raft_membership.voter_raft_ids` 与生成出的 MetadataNode `raft_id` 一致。
  - 验证序列化到 JSON 后再加载，`raft_id`、`node_id` 和初始 membership 保持稳定。

- `cluster_config_validation_test.T070RejectsDuplicateFixedMetadataRaftIdsWithClearDiagnostics`
  - 验证重复 `fixed_metadata_raft_ids` 会返回冲突诊断。
  - 验证错误类型为 `kDuplicateRaftId`，并带出明确的 `field_path`、`node_type` 和 `node_id`。

## 3. 是否发现不合理点 / 警告 / 风险

- `tests/cluster_config_test.cpp` 在本任务开始前已经存在一部分 T070 相关未提交改动；本次是在该基础上补齐并收紧断言，没有扩大到生产代码。
- `specs/008-integrated-object-storage-system/tasks.md` 的工作树 diff 中同时存在 `T074`、`T075` 已被勾选的既有脏改动；本任务只额外将 `T070` 从 `[ ]` 改为 `[X]`，没有处理或回滚这些现有差异。
- 当前测试锁定了默认生成路径下 `raft_id` 按配置顺序生成的行为；如果未来设计明确允许不同但仍稳定的生成策略，需要同步更新 T070 用例和对应契约说明。

## 4. 是否修改 common-risk-notes.md 或 risk-register.md

- 未修改 `common-risk-notes.md`
- 未修改 `risk-register.md`

## 5. 验证命令和结果

执行命令：

```bash
git diff -- tests/cluster_config_test.cpp tests/CMakeLists.txt specs/008-integrated-object-storage-system/tasks.md specs/008-integrated-object-storage-system/task-reports/t070-metadata-raft-id-config-test.md
git diff --check -- tests/cluster_config_test.cpp specs/008-integrated-object-storage-system/tasks.md specs/008-integrated-object-storage-system/task-reports/t070-metadata-raft-id-config-test.md
flock -n /tmp/cqupt_raft_build.lock -c 'cmake --preset debug-ninja-safe && cmake --build --preset debug-ninja-safe --target cluster_config_test'
flock -n /tmp/cqupt_raft_build.lock -c './build/linux/safe/tests/cluster_config_test --gtest_filter=cluster_config_generation_test.T070MetadataRaftIdsStayStableDistinctAndMatchInitialMembership:cluster_config_validation_test.T070RejectsDuplicateFixedMetadataRaftIdsWithClearDiagnostics --gtest_brief=1'
```

结果：

- `git diff`：PASS。确认核心改动落在 `tests/cluster_config_test.cpp`、`tasks.md` 和任务报告；同时可见 `tasks.md` 中存在既有的 `T074`、`T075` 勾选差异，非本任务新增。
- `git diff --check`：PASS。未发现空白错误。
- `cmake --preset debug-ninja-safe && cmake --build --preset debug-ninja-safe --target cluster_config_test`：PASS。
- `./build/linux/safe/tests/cluster_config_test --gtest_filter=cluster_config_generation_test.T070MetadataRaftIdsStayStableDistinctAndMatchInitialMembership:cluster_config_validation_test.T070RejectsDuplicateFixedMetadataRaftIdsWithClearDiagnostics --gtest_brief=1`：PASS，`2` 个 T070 相关用例通过。

通过用例：

- `cluster_config_generation_test.T070MetadataRaftIdsStayStableDistinctAndMatchInitialMembership`
- `cluster_config_validation_test.T070RejectsDuplicateFixedMetadataRaftIdsWithClearDiagnostics`
